# Core (cross-platform)
numpy>=1.24
scipy>=1.10
pandas>=2.0
opencv-python>=4.8
pyyaml>=6.0
ultralytics>=8.1          # YOLO pose; pulls torch
# torch with CUDA on the capture PC (Windows + NVIDIA GPU):
#   python -m pip install --upgrade --force-reinstall torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
dearpygui>=1.10           # GUI (primary backend)
openpyxl>=3.1             # Excel trial export

# Windows capture PC only (force plates):
#   nidaqmx>=0.6           # NI-DAQ; requires NI-DAQmx runtime installed

# Optional:
#   ndi-python             # only if using NDI camera transport
#   opensim                # offline inverse-kinematics refinement
#   pyarrow                # parquet export
